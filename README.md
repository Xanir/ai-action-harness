# ai-action-harness
LangGraph backed harness that provides @Actions (skills + role) and the ability to set what models are called for what Actions. MCP connections can be set to Flash models to save on AI costs.
